aiohttp>=3.9
httpx[asyncio]>=0.27
python-dotenv>=1.0
rich>=13.0
rapidfuzz>=3.0
python-telegram-bot>=21.0
# ML pipeline (ml/ module). scikit-learn pinned so the pickled model
# (ml/artifacts/model.joblib) unpickles on the Fly image without version drift.
pandas>=2.0
numpy>=1.26
scikit-learn==1.8.0
xgboost>=2.0
pyarrow>=15.0
joblib>=1.3
